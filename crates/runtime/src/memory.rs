// pub trait IMemory: wasi_common::wiggle::DynamicGuestMemory {
//     // fn size(&self) -> u32;
//     // fn get_range(&self, ptr: u32, size: u32) -> anyhow::Result<Vec<u8>>;
//     // fn set_range(&self, ptr: u32, data: Vec<u8>) -> anyhow::Result<()>;
// }
